output_dir: "${models_dir}/sft"
per_device_train_batch_size: 1
gradient_accumulation_steps: 8
num_train_epochs: 3
learning_rate: 2e-5
warmup_ratio: 0.05
bf16: true
logging_steps: 2
save_steps: 500
save_total_limit: 3
optim: "adamw_torch"
report_to: "wandb"
max_seq_length: 2048
packing: false
