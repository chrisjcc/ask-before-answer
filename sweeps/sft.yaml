name: sft-bayes-hyperband
description: >
  Bayesian hyperparameter sweep for the train_sft pipeline.
  Searches SFT learning rate while minimizing eval/loss using
  Hyperband early stopping
project: ask-before-answer
program: scripts/run_sweep_trial.py
command:
  - ${env}
  - ${interpreter}
  - ${program}
  - "--stage"
  - "train_sft"
  - ${args}
method: bayes
metric:
  name: eval/loss
  goal: minimize

# Enable Hyperband Early Stopping
early_terminate:
  type: hyperband
  min_iter: 50    # Hyperband's first bracket is based on 50 logged
                  # iterations of the optimized metric (eval/loss).
  eta: 3          # Hyperband bracket multiplier.

parameters:
  learning_rate:
    distribution: log_uniform_values
    min: 0.00001
    max: 0.0005
