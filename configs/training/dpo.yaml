output_dir: "${models_dir}/dpo_output"
per_device_train_batch_size: 1
gradient_accumulation_steps: 8
num_train_epochs: 1
learning_rate: 5e-5
warmup_ratio: 0.1
bf16: true
logging_steps: 10
save_steps: 200
save_total_limit: 2
optim: "paged_adamw_8bit"
report_to: "wandb"
max_prompt_length: 1024
max_length: 2048
beta: 0.1
